var age = 15;
var age = 20; // can redeclare same variable (may accidently override previous value) that declare by var

let myAge = 11;
myAge = 12; // can change the value of variable declared by let

const countryName = 'Bangladesh';
countryName = 'Chander desh'; // can not change the value of variable declare by const
console.log(countryName);